# 1:1 Companion

Internal tool for managing 1:1 meetings between Leaders and Individual Contributors.

## Quick Start
```bash
# Install dependencies
cd api && npm install
cd ../web && npm install

# Setup database
cd ../api && npx prisma migrate dev

# Start development
npm run dev  # In both /api and /web
```

## Documentation

See `/docs/` for full specifications.